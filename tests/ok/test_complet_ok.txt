program {
  real x;
  real y;
  real z;
  bool ok;

  x = 10;
  y = 2;
  z = (x + y) * 3 - y / 2;

  ok = (x > y) && !(z < x);

  if (ok) {
    x = x - 1;
  } else {
    x = x + 1;
  }

  while (x > y) {
    x = x - 1;
    ok = (x >= y) || false;
  }
}